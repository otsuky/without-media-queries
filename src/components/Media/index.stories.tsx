import { ComponentStory, ComponentMeta } from "@storybook/react";

import { Media } from "./";

const meta: ComponentMeta<typeof Media> = {
  component: Media,
};
export default meta;

export const Sample = {};
